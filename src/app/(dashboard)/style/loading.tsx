import { PageSkeleton } from "@/components/ui/page-skeleton";

export default function StyleLoading() {
  return <PageSkeleton title="Styles" subtitle="สไตล์การเขียนของคุณ" variant="grid" count={6} />;
}
