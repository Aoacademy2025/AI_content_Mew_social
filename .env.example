# ClipForge desktop API. Set to 1 to enable every /api/desktop/* route.
# Unset or any other value keeps the surface inert (403 DESKTOP_DISABLED).
DESKTOP_APP=

# Comma-separated Hero AI user ids invited to the desktop app.
# Empty / unset = everyone when DESKTOP_APP=1. Non-empty = only listed ids
# (others get 403 DESKTOP_NOT_INVITED). Never put secrets here.
DESKTOP_ALLOWLIST=

# HMAC-SHA256 key for signed desktop entitlement snapshots (≥ 32 bytes).
# Unset or shorter than 32 bytes → device register/heartbeat fail closed
# (503 DESKTOP_MISCONFIGURED). Never put a real secret here.
DESKTOP_SNAPSHOT_SECRET=

# Desktop STT adapter for POST /api/desktop/transcribe.
# gemini-transcribe = gemini-3.5-transcribe (default)
# hero-chunked = Gemini call used by /api/videos/transcribe
DESKTOP_STT_PROVIDER=gemini-transcribe
