# Handoff — 2026-06-05 (Mew · Payment/pricing vertical)

End-of-session handoff. Everything below shipped to **prod** (studio.heroaiengine.com) today unless marked otherwise. Prod = Hostinger VPS, deploy via `bash deploy/deploy.sh` on `git pull main`.

## ✅ Shipped today (all LIVE on prod)

| # | Feature | PR | Commit | Notes |
|---|---|---|---|---|
| 1 | **renewal-reminders cron** | — | — | `CRON_SECRET` set on prod, `pm2 start … --only renewal-reminders`, runs daily 9:00, `pm2 save`. `cleanup-videos` cron deliberately NOT started. |
| 2 | **CI build check** | #3 | `9ecdd3a` | `.github/workflows/ci.yml` runs `prisma generate` + `npm run build` on every push/PR to main. Non-blocking signal. |
| 3 | **Cancel-at-period-end UI + reactivate** | #4 | `bcd1bc4` | Webhook handles `customer.subscription.updated`; `POST /api/payments/reactivate`; settings banner "แพ็ก … จะยกเลิกวันที่ …" + "ใช้ … ต่อ". Schema: `User.cancelAtPeriodEnd`, `cancelAt`. |
| 4 | **PromptPay payment option** | #5 | `eb4516b` | Pricing page: annual-only method toggle (💳 บัตร·ต่ออัตโนมัติ / 📱 PromptPay·จ่ายครั้งเดียว). **Closes campaign Phase 1.** |
| 5 | **Coupon DISCOUNT type** | #6 | `88d2c60` | % off via Stripe promotion codes. Schema: `Coupon.type/percentOff/discountDuration/stripeCouponId/stripePromotionCodeId`. New `/api/coupons/validate`; checkout attaches `discounts:[{promotion_code}]`; webhook records `CouponRedemption`. **Starts Phase 2.** |

All five were E2E-verified (chrome-devtools + Stripe test mode) before deploy. Specs/plans live in `docs/superpowers/{specs,plans}/2026-06-05-*`.

## ⚠️ Open items / watch-outs

- **Uncommitted pricing-page redesign** in Mew's local working tree: `src/app/(dashboard)/pricing/page.tsx` is modified to ~750 lines (from 439) with a fuller redesign (new icons: Bot/Clapperboard/Film/Music4/Captions/Wallet, extra sections). This is **another session's WIP**, left uncommitted by decision; it already integrates the coupon-discount logic (`appliedCoupon`/`couponCode`/`onDiscountApplied`). PR #6 shipped the **clean 439-line** coupon version, NOT this redesign. Whoever owns the redesign should commit it via its own branch/PR.
- **CI Node-20 deprecation**: bump `actions/checkout@v4` + `actions/setup-node@v4` → v5 before ~2026-06-16.
- **No real-card smoke + refund** on prod yet (Stripe config validated, but no real live transaction). Recommended as final 100% confirmation.
- **Clerk test user** `mewtest+clerk_test@example.com` had its password set + a temporary ADMIN role during E2E (role restored to USER). Test Stripe objects + DISCOUNT/GRANT test coupons were cleaned up.

## 🗺️ Phase 2 remaining (recommended order)

1. **Founding-100 counter (atomic)** — first 100 annual upgraders get founding price; must be race-safe (DB transaction/atomic increment). Builds on the coupon-discount work; needed before launch. *(recommended next)*
2. **Free trial** — community 1 month / public 7 days → on expiry, prompt annual offer.
3. **Claim/allowlist page** — Skool→Zapier email → match → issue code (idempotent).
4. *(Phase 3)* Evergreen **sale page** + Launch Founding-100 page — designed only (`creator studio/sale-page-mockup.html`); not built.

## 🔧 Key technical notes (non-obvious)

- **Stripe API `2026-04-22.dahlia`** (SDK 22.1.1): (a) `subscription.current_period_end` moved into `items.data[]` — use top-level `sub.cancel_at` for cancel dates. (b) Promotion code creation uses `promotion: { type: "coupon", coupon: <id> }`, **not** top-level `coupon:` (build catches the old form). Checkout `discounts:[{promotion_code}]` unchanged.
- **Stripe config lives in DB `SiteConfig`**, loaded by `src/lib/load-stripe-config.ts::ensureStripeConfig()` — call it before any `stripe.*`. Prod `.env` has no Stripe keys. Live webhook endpoint `we_1Tet6N…` already subscribes the 6 needed events incl. `customer.subscription.updated`.
- **Deploy with schema changes**: `deploy.sh` runs `prisma generate` but **NOT `prisma db push`** — for any schema change you must `cp prisma/dev.db prisma/dev.db.bak-…` then `npx prisma db push` on the VPS before/with the build. Additive columns = no data loss.
- **Local dev**: run our app on `npx next dev -p 3005` — **`:3000` is a different project (`content-deck`)**, not this app. Local `.env` has Stripe TEST keys + 6 test price IDs + Clerk test keys (Clerk test: email `*+clerk_test@…`, OTP `424242`).
- **Team**: `main` = prod; Mew works on `mew/*` branches + PR; coordinate deploy timing + shared files (`prisma/schema.prisma`, `package.json`) with `wao1234` (render/AI backend).

## Prod state (verified 2026-06-05)
72 users · 13 payments · 2 coupons · pm2 `ai-content` online (unstable_restarts=0) · DB backups `prisma/dev.db.bak-*` on the VPS.
