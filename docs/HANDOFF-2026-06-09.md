# Handoff — 2026-06-09 (Gemini/keyword/editor bugs + support close-out)

Session นี้ปิด bug จาก support ticket ผู้ใช้จริงของ `bunchar@aoacademy.co` และ `sumawadee@aoacademy.co`, deploy ขึ้น production แล้ว, ตอบ ticket และปิด case แล้ว

> prod app deploy ล่าสุด = `4430835` · studio.heroaiengine.com · PM2 `ai-content` online · product update `v0.4.3` published · ดู [STATUS.md](../STATUS.md) ประกอบ

## ✅ Closed Today

### Bugfix release ที่ขึ้น live
| Commit | สถานะ | หมายเหตุ |
|---|---|---|
| `161e393` | LIVE | Gemini failures, keyword empty-state, transcribe/subtitle duration guard, editor timeline/playhead/zoom/spacebar, notification click |
| `6ba829a` | LIVE | build script returns failure when Next worker dies by signal |
| `4430835` | LIVE | deploy script retries if `.next/BUILD_ID` is missing after build |

### User-facing fixes
- Gemini/API key: friendly error classification for invalid key, API disabled, quota/billing, permission, timeout, high demand.
- Keyword step: no longer reports success with `0 keywords`; quota/API failures now stop the pipeline with a clear message.
- Transcribe/subtitle: source audio duration is authoritative; if Gemini timestamps end far before the audio ends, API returns retryable `422` instead of rendering a clipped/misaligned video.
- Video Editor timeline/playhead: maps video time to caption/audio time, so playhead, active caption, split, seek, and caption clicks stay in sync.
- Timeline zoom: capped UI at `100%`.
- Spacebar: play/pause works in `/video-editor` and video preview modal in `/videos`.
- Notifications: clicking `VIDEO_COMPLETED` notification opens `/videos`.
- Copy/docs/settings: customer-facing copy says to connect/enable Google billing/API key; no “Tier 1” or “System key” wording.

## Support Ticket Close-Out

Production DB was updated directly after deploy:
- All previously open support tickets are now `CLOSED`.
- `SupportTicket` status count after close-out: `CLOSED = 21`, no open tickets remaining.
- 7 open tickets closed this session:
  - `sumawadee@aoacademy.co`: notification click to video page.
  - `bunchar@aoacademy.co`: playhead not following video.
  - `bunchar@aoacademy.co`: Gemini API key not passing.
  - `bunchar@aoacademy.co`: retry keyword still empty.
  - `bunchar@aoacademy.co`: zoom should max at 100%.
  - `bunchar@aoacademy.co`: subtitle not matching audio.
  - `bunchar@aoacademy.co`: spacebar play/pause request.
- Created in-app summary notifications for `bunchar@aoacademy.co` and `sumawadee@aoacademy.co`.
- Did not send seven separate email replies, to avoid spamming users.

## Product Update

Created/published in production:
- Version: `v0.4.3`
- Title: `แก้บัค Video Editor, Gemini และการแจ้งเตือน`
- Category: `FIX`
- Importance: `BANNER`
- State: `PUBLISHED`
- Pinned: `true`
- CTA: `/video-editor`

`/updates` is protected; signed-out curl redirects to login, which is expected. DB verification confirmed the update exists and is published.

## Production Deploy Notes

- VPS: Hostinger, app path `/var/www/ai-content`
- Prod HEAD before deploy: `b3a7bd0`
- Prod HEAD after deploy: `4430835`
- Build ID after deploy: `0lOGRH5EgVv_HG-CBlv5Z`
- PM2: `ai-content` online after restart
- Smoke checks:
  - `/` returned `200`
  - `/api/founding/status` returned `{"active":true,"remaining":99,"total":100,"percentOff":50}`
  - `/video-editor` and `/videos` redirect signed-out users to login as expected

### DB backups from this session
- Before bugfix deploy: `/var/www/ai-content/backups/dev.db.before-161e393.20260609-095200.bak`
- Before support ticket close/update write: `/var/www/ai-content/backups/dev.db.before-support-close-20260609-101005.bak`

### Build/OOM finding
- Production build failed twice before restart because Next build worker was OOM-killed.
- `dmesg` confirmed node RSS around 15.5GB and swap was exhausted.
- Existing swap: `/swapfile-codex-build` 8GB.
- Added persistent swap: `/swapfile-codex-build-extra` 24GB and added it to `/etc/fstab`.
- After deploy, swap total was about 31GB and app was stable.
- Disk after extra swap had about 41GB free; watch disk pressure if adding large media/artifacts.

Latest verified deploy command pattern:

```bash
BUILD_HEAP_MB=4096 BUILD_WORKER_HEAP_MB=512 BUILD_HEAP_MB_LOW=3072 BUILD_WORKER_HEAP_MB_LOW=512 BUILD_NO_LINT=1 bash deploy/deploy.sh
```

Expected build warnings remain:
- `@ffmpeg-installer/ffmpeg` critical dependency warning.
- `fluent-ffmpeg` critical dependency warning.
- ESLint/Next plugin warning locally.

## Investigation Notes Worth Keeping

- Bunchar account was PRO ADMIN with Gemini/Pexels/Pixabay keys and Gemini TTS provider.
- Root cause of keyword bug was not only user API quota; app behavior was also wrong because it could mark keyword step done with empty results.
- Gemini free-tier error observed in prod logs: `429 RESOURCE_EXHAUSTED`, quota metric `generate_content_free_tier_requests`, free-tier daily limit 20 for `gemini-2.5-flash`.
- Gemini `503 high demand` also observed; UI now surfaces a clearer retry message.
- For subtitle mismatch, prod evidence showed TTS WAV about `45.880s` but transcribed captions/render config about `35.2s`; the last 10 seconds of audio was not silence. New transcribe guard rejects incomplete captions.
- Avatar intro/outro duplicate bug was already fixed before this session by `644c85b`; do not reopen unless there is new post-`644c85b` evidence.
- B-roll slowness already had deployed improvements (`132b79a`, `061c0b9`); current stance is monitor via insights, not a confirmed open bug.

## Current Repo State At Close

- Branch: `main`
- Local `git status --short`: only `?? docs/heroai-mcp-server-plan.md` remains, and it pre-existed this work.
- No active deploy/SSH sessions left by Codex.
- Latest commits on local/main:
  - `4430835 fix(deploy): retry when build artifact is missing`
  - `6ba829a fix(deploy): fail build script on worker signal`
  - `161e393 fix(video): handle gemini failures and sync editor timeline`

## Suggested Next Session

1. Monitor `/admin/insights` and support tickets for post-deploy Gemini/keyword/subtitle signals.
2. Update user manual/docs when Mew is ready.
3. If new tickets mention ElevenLabs, investigate logs showing invalid ID like `bossthaimt@gmail.com` being used as a voice id.
4. Keep an eye on disk usage because persistent build swap added 24GB.
