# Handoff — 2026-06-07 (Mew · Payment/pricing vertical)

Session นี้ปิด Phase 1–3 ของ payment vertical: **ship 3 ฟีเจอร์ขึ้น prod + เปิดแคมเปญ Founding-100 จริง + เปิด cron ครบ** ทุกอย่างทำตาม flow `brainstorm → spec → plan → subagent-driven build → review → PR` และผ่าน E2E/verify ก่อน deploy

> prod ตอนนี้ = `main` `51d8cee` · studio.heroaiengine.com · ดู [STATUS.md](../STATUS.md) + [CLAUDE.md](../CLAUDE.md)

## ✅ ขึ้น prod แล้ววันนี้ (LIVE ทั้งหมด)

| # | งาน | PR | สถานะ |
|---|---|---|---|
| 1 | **Pricing redesign × Founding-100** — หน้า `/pricing` ใหม่ (rich) + founding bar/ราคา founding บนการ์ดรายปี | #10 | 🟢 LIVE |
| 2 | **Sale page** — homepage `/` = evergreen sale page (ราคาสด + founding bar read-only) | #11 | 🟢 LIVE |
| 3 | **Free trial** — สมัครได้ PRO 7 วันอัตโนมัติ → หมด revert FREE + prompt อัปเกรด | #9 | 🟢 LIVE |
| 4 | **Founding-100 LAUNCHED** — สร้าง coupon `FOUNDING100` live (Stripe+DB) → bar โชว์ "เหลือ 100/100" สาธารณะ | — | 🔥 เปิดแล้ว |
| 5 | **Cron เปิดครบ** — `trial-expiry` + `founding-sweep` + `renewal-reminders` (pm2 save) | — | ✅ |

### รายละเอียดสำคัญ
- **#9 Free trial** เคย CONFLICT กับ main (branch ตามหลัง 15) → reconcile โดย merge main เข้า: conflict โค้ดจริง **0** (ชนแค่ `STATUS.md` docs) · `ecosystem.config.js` + `user/me` auto-merge (verify มือแล้ว) · เพิ่มคอลัมน์ `User.trialStartedAt/trialEndsAt`
- **Founding-100** สร้างผ่านสคริปต์ one-off บน VPS (replicate `POST /api/admin/coupons` DISCOUNT path): Stripe coupon `8uQNZqlA` + promo `promo_1TfQ1XL39kyExJWOMei5nylu` · DISCOUNT 50% / forever / maxUses 100 / usedCount 0

## 🧪 Verify ที่ทำ (ก่อน+หลัง deploy)
- unit (tsx throwaway DB): `verify-trial` **12/12** · `verify-founding` **19/19** · `verify-pricing-display` **9/9**
- build SUCCESS + tsc clean ทุก PR (build-gate ก่อน push main เสมอ)
- per-task spec+quality review + final holistic review ทุก PR
- **E2E (chrome-devtools):**
  - trial: signup-lazy-create → `/api/user/me` = PRO + trialEndsAt = **+7 วันเป๊ะ** + dashboard banner "ทดลอง PRO เหลือ 7 วัน"
  - pricing/sale page: render ครบ, toggle รายเดือน/ปี, ราคา founding ฿2,995, ราคา amortized ฿499/825, CTA→register, founding bar ซ่อนตอน inactive
  - หลังเปิด founding: หน้าแรก prod โชว์ bar "เหลือ 100/100" จริง (screenshot)
  - หมายเหตุ: signup UI เต็มรูปแบบ automate ไม่ได้เพราะ **Cloudflare Turnstile** (กัน bot — คนจริงผ่านได้)

## 🔧 หมายเหตุเทคนิค (non-obvious — สำคัญสำหรับ session หน้า)
- **`deploy/deploy.sh` รัน `prisma db push` (additive) ก่อน build+restart** → ฟีเจอร์ที่เพิ่มคอลัมน์ deploy ได้ปลอดภัย (คอลัมน์มาก่อนโค้ด ไม่ 500) · **ไม่มี `--accept-data-loss`** (destructive จะ fail loudly)
- **Cron ต้อง `CRON_SECRET` ตอน pm2 start** ไม่งั้น 401: `export CRON_SECRET="$(grep ^CRON_SECRET= .env | cut -d= -f2-)"` แล้ว `pm2 start ecosystem.config.js --only <X> --update-env && pm2 save` · `cleanup-videos` ตั้งใจปิดไว้
- **prod `.env` `DATABASE_URL` = absolute** (`file:/var/www/ai-content/prisma/dev.db`) · `prisma/*.db` gitignored → `git pull`/deploy ไม่แตะข้อมูล user · `public/music/*` เป็น untracked (pull ไม่ลบ)
- **founding bar 2 ที่ทำงานต่างกัน:** หน้าแรก (`src/app/page.tsx`) อ่าน **server-side `getFoundingCoupon` (read-only)** → โชว์ทุกคน · `/pricing` ใช้ **client-fetch `/api/founding/status`** ซึ่ง**ไม่ใช่ public route** → โชว์เฉพาะคนล็อกอิน
- **สร้าง discount coupon เพิ่ม:** prod `/admin/coupons` (ต้อง role ADMIN) หรือ VPS script แบบเดียวกัน (ต้อง `ensureStripeConfig()` ก่อน `stripe.*` เพราะ key อยู่ใน DB `SiteConfig` ไม่ใช่ env)
- **verify scripts** ใช้ throwaway DB: ตั้ง `DATABASE_URL` ไป temp db (path ไม่มี space) → `prisma db push` → `tsx scripts/verify-*.ts` (`verify-trial` มี `deleteMany` → ห้ามรันใส่ dev.db จริง)

## ⚠️ Watch-outs / ที่ต้องรู้
- **renewal-reminders เปิดแล้ว** → ส่งอีเมลลูกค้า manual-renew อัตโนมัติทุก 9โมง (14/7/1 วันก่อนหมด) · start-run ส่ง 0 ฉบับ (ไม่มีใครถึงเกณฑ์)
- **Founding เปิดรับจริง** → 100 คนแรกอัป annual ได้ 50% ตลอดชีพ (จับตา usedCount/ที่นั่ง)
- **OG image ยังไม่มี** (`public/og.png`) → sale page ไม่มี preview ตอนแชร์ LINE/FB
- (option) ทำ `/api/founding/status` เป็น public ถ้าอยากให้ founding bar บน `/pricing` โชว์คน logged-out ด้วย
- **local dev.db** โดน wipe/reseed ตอนเทส (local เท่านั้น prod ปลอดภัย) → ถ้าจะ dev local รัน `npx prisma db push` ก่อน

## 🧭 Branch / repo state
- เหลือ branch เดียว: `main` · PR #9/#10/#11 **MERGED** + ลบ branch ครบ (local+remote) · ไม่มี PR ค้าง
- ลบ branch ตายแล้วไปด้วย: coupon-discount, quick-wins, subscriptions, founding-100, ci-setup

## ⏭️ Phase ถัดไป (แนะนำ)
1. **Claim/allowlist page** — รอ center DB API เช็ค member (Mew กำลังทำ) → เรียก `grantTrial(id,30)` + ออกโค้ด founding/discount เฉพาะตัว (idempotent)
2. **OG image** + (option) /pricing founding bar public
3. **Infra (ทีม wao):** render queue + enforce PRO/BUSINESS clip caps

## Team
`main` = prod (VPS deploy จาก main) · Mew = payment/pricing vertical · wao1234 = render/AI backend · coordinate ไฟล์ร่วม: `prisma/schema.prisma`, `package.json`, `ecosystem.config.js`, `next.config.ts`
