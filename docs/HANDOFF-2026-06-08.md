# Handoff — 2026-06-08 (Video editor audio/subtitle UX + customer backlog)

Session นี้ปิด bug ฝั่ง Mew ของ `/video-editor`: BGM กลับมา, subtitle ไม่ซ้อน, และ UX กลับไปเป็น preview-first/editor-first แล้ว

> prod app deploy ล่าสุด = `e69f589` · studio.heroaiengine.com · Mew test live ผ่าน 2 clips แล้ว · ดู [STATUS.md](../STATUS.md) + [CLAUDE.md](../CLAUDE.md)

## ✅ Closed Today — Mew Video Editor Bug

### Problem เดิม
- หลังแก้ BGM หาย ทำให้ต้องรอ `Burn Subtitles` เสร็จก่อนถึงจะได้เสียงเพลงใน preview
- UX ผิดจากงานตัดต่อจริง: user ต้องปรับสี/font/style/ตำแหน่ง/ตัดคำ subtitle นาน แต่ระบบ auto burn ต่อทันทีหลัง render
- ก่อนหน้านี้มีอาการวน bug:
  - BGM หายใน final/avatar path
  - subtitle ซ้อนหลังย้ายตำแหน่ง
  - แก้ซับซ้อนแล้ว preview กลับไปเล่น base/composite ที่ไม่มี BGM

### Deploy chain ที่เกี่ยวข้อง
| Commit | สถานะ | หมายเหตุ |
|---|---|---|
| `b6bf434` | LIVE | preserve BGM after avatar composite |
| `5e78754` | LIVE | show burned avatar output in editor |
| `f720314` | LIVE | avoid duplicate subtitle preview after burn |
| `014c6e2` | LIVE | show burned preview after subtitle burn |
| `e69f589` | LIVE | separate preview render from subtitle burn |

### Current UX หลัง `e69f589`
1. กด `Render` แล้วหยุดที่ editable preview
2. Preview base ต้องมีเสียงครบ: voice/avatar + BGM
3. Subtitle เป็น live overlay ปรับตำแหน่ง, font, สี, style, keyword, timing ได้
4. ระบบไม่ auto `Burn Subtitles`
5. กด `Burn & Download` ตอนท้ายเท่านั้นเพื่อ export final MP4 ที่ฝังซับ
6. ถ้า render/composite base ใหม่ จะ clear burned output เก่า เพื่อไม่ให้ preview เล่นไฟล์ final รอบก่อนผิด context
7. Editable preview save เป็น `PROCESSING`; burn สำเร็จค่อย update เป็น `COMPLETED`

### Verify
- Local: `npm run lint` ผ่าน
- Prod build ผ่าน
- Prod HEAD verified: `e69f589`
- PM2 `ai-content` online
- `/video-editor` signed-out request ตอบ `307` ไป login ตามปกติ
- Mew test live 2 clips: preview audio + burn subtitle ทำงานโอเค

## 🔧 Key Code Context

- Main editor flow: `src/app/(dashboard)/video-editor/page.tsx`
  - `runRender()` ตอนนี้สร้าง no-sub editable preview + BGM และ save `PROCESSING`
  - `runComposite()` ตอนนี้เป็น editable avatar preview และ save `PROCESSING`
  - `runAll()`, `runFrom()`, `runRenderOnly()`, `runAvatarPipeline()` ไม่ auto burn แล้ว
  - `burnSubtitlesCore()` คือ final export path
  - `clearDerivedPreviewOutputs()` clear burned/composite state เมื่อ base เปลี่ยน
- Subtitle burn composition: `src/remotion/SubtitleOverlayComposition.tsx`
- Render API: `src/app/api/videos/render/route.ts`
- Avatar composite API: `src/app/api/heygen/composite/route.ts`

## ⏳ Backlog Tomorrow — Customer `kapokja@gmail.com`

Hold ไว้ตามคำสั่ง Mew. ลูกค้าคนนี้เป็น paid annual PRO customer รายแรก ๆ และต้องกลับมาทำต่อพรุ่งนี้

### Account facts from prod audit
- Email: `kapokja@gmail.com`
- Plan: `PRO`
- Billing: annual/manual topup case handled
- Plan expiry BKK: `2027-06-06`
- `subStatus=canceled` ยังดูแปลกจาก historical monthly/cancel/manual topup flow แต่ไม่ใช่ root cause ของ render/subtitle/avatar bug

### Customer symptoms
1. Avatar เห็นครึ่งตัว/ตำแหน่งไม่ตรง preview
2. Render avatar ค้างเป็นชั่วโมง
3. No-avatar render แล้ว subtitle ไม่ขึ้น มีแต่วิดีโอ

### Root cause audit summary
1. **Avatar half-body / positioning**
   - `OrderPanel` ใช้ offset `-2..2`, scale default `2.02`
   - `RightSettingsPanel` ใช้ offset `-200..200`, reset scale ประมาณ `0.35`
   - state ตัวเดียวกันถูกส่ง raw เข้า HeyGen
   - composite route ยังไม่ได้ apply `avatarScale/avatarOffsetX/avatarOffsetY` จริงทุก path
   - Files:
     - `src/app/(dashboard)/video-editor/_components/OrderPanel.tsx`
     - `src/app/(dashboard)/video-editor/_components/RightSettingsPanel.tsx`
     - `src/app/(dashboard)/video-editor/page.tsx`
     - `src/app/api/heygen/composite/route.ts`

2. **Avatar/render stuck**
   - พบ stale job ของ user นี้: `cmpxv4zse000ylce2ee540l9i-1780812039755`
   - เริ่ม `2026-06-07 13:00:39 BKK`, progress ค้าง `1%`
   - ถูก job ใหม่ supersede ประมาณ `13:15 BKK` แต่ job file ยังเป็น `running`
   - render job cancellation/supersede path ยังต้อง mark cancelled/error + write progress error ให้ UI หลุดจากค้าง
   - HeyGen polling ฝั่ง browser ยังหยุดรอตอน `document.visibilityState === "hidden"` ทำให้ลูกค้ารู้สึกว่าค้างยาวได้
   - Files:
     - `src/app/api/videos/render/route.ts`
     - `src/app/api/videos/render-status/route.ts`
     - `src/app/api/videos/render-progress/route.ts`
     - `src/app/(dashboard)/video-editor/page.tsx`

3. **No-avatar subtitle missing**
   - ก่อน `e69f589` flow render รอบแรก intentionally no-sub แล้ว auto burn; old gallery records อาจถูก save เป็น no-sub `COMPLETED` ถ้า burn skipped/hang
   - `kapokja` DB records มี `renderConfig.keywordPopups` จริง ไม่ใช่ transcribe/config ไม่สร้างซับ
   - งานใหม่หลัง `e69f589` ลดความเสี่ยงนี้เพราะ preview save เป็น `PROCESSING` และ final ต้องมาจาก Burn แต่ historical customer videos ยังต้อง recovery/backfill แยก

### Suggested P0 tomorrow
1. Normalize avatar position controls ให้เหลือ unit เดียว และ reset ค่าเดียวกันทุก panel
2. ให้ composite route apply `avatarScale/avatarOffsetX/avatarOffsetY` จริง หรือซ่อน control ที่ยังไม่ support
3. แก้ render cancellation/supersede ให้ job เก่าถูก mark `cancelled/error` และ progress file มี error ทันที
4. เพิ่ม stale timeout สำหรับ burn/render progress ฝั่ง UI/API เพื่อไม่ค้างหลอกเป็นชั่วโมง
5. Customer recovery: หา video id ที่ลูกค้าต้องการ แล้ว re-burn/rerender จาก `renderConfig` หรือให้ลูกค้า rerun หลัง fix พร้อม monitor logs

## Prod Deploy Notes

- VPS: Hostinger, app path `/var/www/ai-content`
- Do not store SSH password in repo/docs
- Latest verified deploy command pattern:
  `BUILD_HEAP_MB=4096 BUILD_WORKER_HEAP_MB=512 BUILD_HEAP_MB_LOW=3072 BUILD_WORKER_HEAP_MB_LOW=512 BUILD_NO_LINT=1 bash deploy/deploy.sh`
- Swap file from earlier session is active for build safety (`/swapfile-codex-build`, 8G)
- Expected build warnings: `@ffmpeg-installer/ffmpeg` and `fluent-ffmpeg` critical dependency warnings; seen before and not related to this bug

## Session Close State

- Branch: `main`
- Prod app deploy verified by Codex: `e69f589`
- Repo main contains docs close-out on top of `19f70e2`
- Note: origin/main received `19f70e2 feat(video-editor): auto-load avatar preview + subtitle polish` after the `e69f589` prod deploy. Codex rebased the docs close-out on top of it but did not deploy that code in this close-out step.
- No active SSH sessions left by Codex
- Current Mew bug: closed
- Next work: customer backlog above
