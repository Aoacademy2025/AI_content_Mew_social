# AI image engines do not cross-fallback

RunPod AI and Cloud API are separate customer-selected AI Engines, not primary and fallback providers. A generation job is quoted, charged and executed entirely inside the selected engine; failure ends that job under its refund rules. Choosing another engine is a new explicit product choice with its own model and price, which keeps COGS, availability and customer expectations independent.
