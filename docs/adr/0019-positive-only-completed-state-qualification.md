# Positive-only completed-state qualification remains fail-closed

Date: 2026-08-17
Status: Accepted

The paid hard-facts and lettering v8 probe passed nine of twelve strict visual
checks. It fixed the exact-one-boat regression across all five active formats
but still produced two water glasses, malformed captions in Clear Infographic
and visible water under a supposedly repaired tap.

New work therefore publishes `cinematic-realism-v8`,
`simple-editorial-story-v9`, `dramatic-comic-v8`, `clear-infographic-v8` and
`retro-story-v8`. The compiler makes established essential-object quantities
explicit, translates completed failure states into affirmative visible states,
and gives Clear Infographic a compact object-only composition with uninterrupted
background around each pictogram. Content Preflight likewise records visible
essential-object quantities when the Narrative Source establishes them.

The prior v7 and v8 recipes and their paid benchmark fixtures remain immutable.
The benchmark harness reconstructs both stored prompt sets exactly rather than
compiling them through the current catalog or silently upgrading an old pin.

An approved eight-image public RunPod `z-image-turbo` probe completed without a
provider failure or retry. Seven images passed: one boat across all five active
formats, one water glass with three circles, and the caption-free Clear
Infographic comparison. The dry-tap image still contained a small translucent
stream and drop at original resolution even though the provider prompt did not
name water, dripping, drops or flow.

The completed-state gate therefore remains fail-closed. The paid 120-image
matrix is not authorized, no image is retried automatically, and no fallback
engine is selected. A later recipe must stage the completed inspection as a
different affirmative pose and requires a separately approved focused paid
probe before qualification can continue.
