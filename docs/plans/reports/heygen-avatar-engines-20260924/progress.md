# Execution ledger
Plan: docs/plans/2026-09-24-heygen-avatar-engines.md
Profile: high-assurance; automatic fix rounds max5; child runs max16; numerical usage unavailable.

- User approved IV/V direction/execution; keep existing BYOK disclosure and explicit choice, no new billing policy. Fresh worker contexts satisfy execution separation; no redundant approval/session restart.
- Official research + focused integration map complete; research follow-up establishes v3greenMP4 and private per-look capability metadata. Three substantive research/map runs used.
- Separate existing PR539/540/541 deployment is explicitly authorized and owned by deploy_ops_three; this worktree must not mutate prod or merge newfeature during that release.
- Heavy implementation worker started from55eced5b with approved spec; substantive feature child runs now4 (three research/map plusimplementation). No review-driven fixround yet; numerical usage unavailable.
- Worker milestone: observed RED/GREEN for private v3 cursor catalog/engines, v3 audio/create limits and request contract, versioned poll/checkpoint/resume, and BYOK receipt. Production-path integration/UI verification still in progress; mocks do not prove live provider success.
- Additional RED found legacy cache scope used only final6keycharacters; worker changed per-account cache identity to full-key SHA256 and added keyrotation/cursor regression. Old drafts keep absent engine field for legacy III; new drafts explicitIII. Fresh compatibility applies before paid provider work.
- Separate authorized3PR release completed production55eced5b at2026-09-23 18:04:36UTC; health/workers/gates passed. HERO52Doneguidance scope, HERO41awaitsnaturalcleanupverification, HERO10actualholderunknown. That release does not include HERO54. Old merged worktrees removed throughOrca; activefeature dependencies must remain selfcontained/stable.
- Mew authorized paid realavatarQA (“ให้เจน avatar qa test ได้เลย”). Requested required account/avatar identity asynchronously; pending. Added bounded postreviewQA stage: shortsyntheticnarration, maxonecreate per selectedcompatibleIV/V, noautopaidretry or featuredeploy. Automated implementation/review continues independently.

- Follow-up supersedes the earlier pending identity/timing entry: owner identity verified privately in Chrome. User requested immediate empirical generation during review. Coordinator bounded paid scope to two distinct IV experiments on the same private completed default look with local 8.333second narration; exactly two creates completed, no retries/fallback/III/V. Baseline retained source-room pixels; adding remove_background=true removed them and passed simple chromakey inspection. This proves provider behavior for this look, not final app integration or the original incident cause. Credentials removed; no further paid creates.
- Draft PR542 opened at db026a89. That head passed local build and GitHub CI35905416007, but independent task/correctness/security reviews identified blockers: unknown-paid-outcome refund through outer orchestrator, changed asset/body under the same idempotency key, malformed present web engine defaulting to III, unknown capability misclassification, and lost quota classification. Fixround1 is active with narrow pagination/cache/log/path advisories and the empirically validated removal flag.
- Approved durable upload seam: prepare/upload audio, persist separate intro/tail asset IDs before paid create, build each logical request from pinned values, and never resubmit an unknown outcome on resume. Known requested engines in wellformed metadata remain positive evidence even alongside future unknown strings.
- Feature child runs used11 of16; final three independent re-review runs remain after stable fix head. Numeric usage unavailable. Separate PR539/540/541 release is complete; no feature merge/deploy approval inferred.
