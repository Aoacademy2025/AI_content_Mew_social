# Release test review — PASS

Reviewed `d9b7f22b^..d9b7f22b` only. The sole change adapts the launch asset check from the removed compact-copy literal (`5 ขั้นตอน`) to exactly five `title:` declarations in `HeroScriptQuickStart`, while retaining the mounted-component and `/docs/hero-script` assertions. The component declares the original five beginner steps (setup, topic, Hook, script, edit), defaults its optional guide to collapsed, and persists an explicit account-scoped expand/collapse preference. No source behavior, race/ownership evidence, runtime, schema, entitlement, quota, or MAPC code changed. `npx tsx scripts/verify-hero-script-launch-assets.ts` passed (31 checks); `npm run verify:hero-script-workspace` passed, including the mounted guide preference interaction. This repairs the presentation-coupled CI contract without hiding missing approved guidance.

## `671eeec7` — PASS

Reviewed `671eeec7^..671eeec7` only. Its sole change adds Puppeteer’s existing CI-only sandbox arguments, `--no-sandbox` and `--disable-setuid-sandbox`, to the Hero Script workspace browser fixture. The expression is byte-for-byte consistent with the established `verify-brands-mobile.mjs` and `verify-brand-setup-ui.mts` pattern. GitHub Actions supplies `CI: "true"`; without that variable the test continues to launch with an empty argument list, so local behavior is unchanged. The repair affects only disposable test Chromium, with no production launch, runtime, schema, entitlement, quota, MAPC, or customer-data path. Independent `CI=1 npm run verify:hero-script-workspace` passed both the workspace and browser checks. The scoped command directly exercises the repaired launch seam and is sufficient for this one-line CI fix.
