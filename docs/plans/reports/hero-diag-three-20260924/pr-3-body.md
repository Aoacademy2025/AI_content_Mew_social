Add an offline benchmark around the exact existing acoustic engine, plus a noncustomer Thai speech fixture generator and CI tests for deadline handling, output validation and fixture boundaries. The production acoustic algorithm, budgets, quality thresholds and fallback/export behavior are unchanged.

[HERO-51](https://linear.app/mew-social/issue/HERO-51) has natural long-clip acoustic timeouts in emissions. The pinned actual model on an Apple M4 completed repeated synthetic Thai audio of 121/200/272/302 seconds in 11.35/17.71/23.75/28.09 seconds under the existing 60-second deadline, with roughly 3.4 GB sampled peak RSS. This does not reproduce the production timeout or establish human subtitle acceptance; no speculative chunking change is included.

Validation: Python engine/fixture/harness tests and acoustic clock/worker verifiers pass; actual model inference used offline flags and exact pinned revision. Reports validate identity, duration, monotonic timestamps, eligible-character coverage and synthetic repetition boundaries. Independent task re-review, whole-branch correctness and security/privacy reviews are clear. The integrated production build passed; final-head GitHub CI passed (10m54s). A preview-progress assertion failed once in a broader run; isolated replay passed 108/108 on both an archived base and branch HEAD. The combined broader suite also passed.

No customer media, weights or generated artifacts are committed. Next gate is the same noncustomer benchmark on deployment-equivalent hardware/load. No production inference, provider spend or deployment occurred.

Review fix: exact eligible-span completion (no omissions, duplicates or unexpected spans) and complete nonempty known boundaries now gate benchmark acceptance. A public-child regression first reproduced the previous erroneous aligned result with only 1/6 spans; the fixed harness rejects it. All seven harness tests pass, and the stored measured matrix satisfies the strengthened checks. Offline flags cover the model libraries; this is not an OS network sandbox. Timeout controls the trusted direct engine process, not arbitrary descendant processes.
