# Insights Page Redesign — Design Doc

> **Audience:** Mew (CEO) + dev
> **Status:** Proposal (2026-06-18) — ยังไม่ลงมือ
> **Goal:** ให้หน้า `/admin/insights` ตอบ 2 เรื่องคู่กัน — **คนใช้งาน** + **ระบบ** — และเชื่อมกันได้ ว่า dropoff ตรงไหนเพราะ "UX งง" หรือ "ระบบช้า" เพื่อให้ตัดสินใจพัฒนาได้ถูกจุด
> **Baseline data:** snapshot prod 2026-06-18 (ดู Appendix A)

---

## 1. TL;DR (อ่าน 30 วินาที)

หน้าปัจจุบัน **ข้อมูลดิบดีอยู่แล้ว** (มี p50/p95 ต่อ stage, b-roll phases, queue, memory, playback, vitals) แต่ **เรียงผิดลำดับ + เชื่อถือไม่ได้บางจุด** เลยชี้ไปผิดทาง:

- โชว์ **"Video completed 100%"** (ดูสมบูรณ์แบบ) แต่บังความจริงว่า **คน 200 คน มีแค่ 18 (9%) ที่เคยได้วิดีโอจริง**
- โชว์ **"หลุด 96% ที่เริ่มจากสคริปต์"** ซึ่ง **เป็นตัวเลขปลอม** (เกิดจาก bug หน่วยข้อมูล — ดู §3)
- **Health Score 20** (ดูวิกฤต) เพราะรวม funnel ปลอม + error ของคีย์ลูกค้า + noise เข้าไปคิด

**ข้อเสนอ:** ไม่แยกเป็น 2 หน้า แต่รวมเป็น **"เส้นทางเดียว: สมัคร → วิดีโอเสร็จ"** ที่ทุกจุดแสดง 2 มุม ("คนผ่านกี่%" + "ทำไมคนหาย: UX/ช้า/error") แบ่งหน้าเป็น **4 บล็อค** + **4 กฎความน่าเชื่อถือ**

**สิ่งที่ข้อมูลบอกว่าต้องแก้ (เรียงตาม leverage):**
1. 🥇 **คน** — 72% ของลูกค้าจ่ายเงินไม่เคยตั้ง Gemini key → ใช้ไม่ได้ตั้งแต่ต้น
2. 🥈 **ระบบ** — b-roll/render หางช้า (p95 18–24 นาที) → คนที่เริ่มแล้วทิ้ง 54%
3. 🥉 **หน้า insight เอง** — restructure ตามเอกสารนี้

---

## ศัพท์ที่ใช้ในเอกสารนี้ (Glossary — อ่านก่อน)

> เราคงศัพท์มาตรฐานของวงการไว้ (เพื่อให้ dev/อนาคตคุยกับเครื่องมืออื่นรู้เรื่อง) แต่ใส่คำอธิบายภาษาคนกำกับ เพื่อให้ CEO อ่านวิเคราะห์เองได้

| ศัพท์ | แปลว่าอะไร (ภาษาคน) |
|---|---|
| **Funnel (กรวย)** | มองผู้ใช้เป็นขั้นบันได ขั้นถัดไปคนน้อยลงเรื่อยๆ — ใช้ดูว่า "คนหล่นหายตรงขั้นไหน" |
| **Activation (การเริ่มใช้งานจริง)** | สัดส่วนคนที่ได้ "คุณค่าครั้งแรก" = ได้วิดีโอเสร็จ 1 ตัว (ตัวชี้วัดว่า product ใช้งานได้จริงไหม) |
| **Drop-off / Conversion (อัตราหลุด / อัตราผ่าน)** | % คนที่หาย / ผ่าน จากขั้นหนึ่งไปขั้นถัดไป |
| **Retention (กลับมาใช้ซ้ำ)** | คนที่ทำวิดีโอ ≥2 ตัว = ติดใจ |
| **p50 / p95 (เปอร์เซ็นไทล์)** | p50 = ค่ากลาง (ครึ่งนึงเร็วกว่านี้) · p95 = หาง (95% เร็วกว่านี้ อีก 5% ช้ากว่า) → **p95 = ประสบการณ์แย่ที่คนเจอบ่อยพอควร สำคัญกว่าค่าเฉลี่ย** เพราะค่าเฉลี่ยถูกตัวเร็วกลบ |
| **Pipeline / Stage (สายการผลิต / ขั้นตอน)** | ลำดับเจนวิดีโอ: keyword → เสียง → ซับ → B-roll → จัดคลิป → เรนเดอร์ → ฝังซับ |
| **Run (รอบเจน 1 ครั้ง)** | การกดสร้างวิดีโอ 1 ครั้ง |
| **BYOK (Bring Your Own Key — ลูกค้าใช้คีย์ตัวเอง)** | ลูกค้าใส่ API key ของตัวเอง (Gemini ฯลฯ) ถ้าไม่ใส่/คีย์มีปัญหา = ระบบเจนไม่ได้ **แต่ไม่ใช่บั๊กของเรา** |
| **Bottleneck (คอขวด)** | ขั้นที่ช้าสุด ทำให้ทั้งงานช้าตาม |
| **Queue wait (เวลารอคิว)** | งานเจนต้องรอเครื่องว่างก่อนเริ่ม (เครื่องทำทีละงาน) |
| **Cache hit (ใช้ของเก่าซ้ำ)** | B-roll ที่เคยโหลดแล้วใช้ซ้ำได้ ไม่ต้องโหลดใหม่ = เร็วขึ้น/ถูกลง |
| **Concurrency (งานพร้อมกัน)** | เรนเดอร์พร้อมกันได้กี่งาน |
| **Health Score (คะแนนสุขภาพระบบ)** | คะแนนรวม 0–100 บอกระบบโดยรวมโอเคแค่ไหน |
| **Telemetry (ข้อมูลการใช้งานที่ระบบเก็บเอง)** | event ที่บันทึกว่าใครทำอะไรเมื่อไหร่ — เป็นวัตถุดิบของหน้านี้ทั้งหมด |
| **Server-authoritative (ยึดข้อมูลจากเซิร์ฟเวอร์)** | นับผลจากฝั่งเซิร์ฟเวอร์ ไม่ใช่ฝั่งเบราว์เซอร์ที่หายได้ตอนปิดแท็บ |
| **Web Vitals (LCP/INP/CLS)** | มาตรวัดความเร็ว/ลื่นของหน้าเว็บฝั่งผู้ใช้ (โหลดเร็วไหม · กดแล้วตอบไวไหม · ภาพเด้งไหม) |

---

## 2. หลักการออกแบบ: หนึ่งเส้นทาง สองเลนส์

Mew พูดถูกว่า "คน" กับ "ระบบ" พันกัน — UX งงทำให้ dropoff, ระบบช้าก็ทำให้ dropoff เหมือนกัน ดังนั้น**ห้ามแยกเป็น 2 หน้า** เพราะจะอ่านแล้วเชื่อมไม่ได้

แทนที่ด้วย **เส้นทางเดียว** ที่แต่ละจุดมี 2 เลนส์ทาบกัน:

```
        ┌─────────── เลนส์ "คน" ───────────┐   ┌──────── เลนส์ "ระบบ" ────────┐
ขั้น →   คนเข้า   คนผ่าน   drop%   สาเหตุที่เดา      สำเร็จ%   error    p50/p95(ช้า?)
```

อ่านแถวเดียวรู้เลย: *"ขั้น B-roll คนผ่าน 93% แต่ p95 = 24 นาที → คนที่หายตรงนี้เพราะ **รอนาน** ไม่ใช่ UX งง → ไปแก้ perf"* ✅

---

## 3. ทำไมหน้าปัจจุบันถึงเชื่อไม่ได้ (หลักฐาน)

### 3.1 Bug หน่วยข้อมูลใน funnel → "96% ปลอม"
`summarizeEditorFunnel()` (`route.ts:264`) นับ:
- **ขั้น 1** (`editor_opened`) = นับ **raw event** ทั้ง history → 655 (14 วัน)
- **ขั้น 2–7** = de-dup ด้วย `pipelineRunId` (`route.ts:271`) แต่ **field นี้เพิ่งเริ่มเก็บ 18 มิ.ย. 07:58 น.** มีข้อมูลแค่ ~5 ชม. → 25 run

→ "655 page-open (14 วัน) หาร 25 run (5 ชม.)" = **คนละหน่วย + คนละช่วงเวลา** → 96% ไม่ใช่คนหลุดจริง และตัวเลขขั้น 2–7 นิ่งเท่ากันทุก window (24ชม=7วัน=14วัน) เพราะข้อมูลมีแค่ 5 ชม.

### 3.2 Stage success% เพี้ยนเพราะ client-event loss
ขั้น tts (7 วัน): started 211, done 132 → "63%" แต่ **err = 0** ของจริงคือ event `pipeline_step_done` ยิงจาก **client** (ปิดแท็บ = event หาย 34 ครั้ง) ไม่ใช่ TTS พัง

### 3.3 Error ปนกัน 3 ประเภทไม่แยก
`summarizeIssueGroups()` รวม "ระบบพัง" + "คีย์ลูกค้า" + "noise" เป็นกองเดียว → recommendation ได้แค่ *"error ที่ 'ทั่วไป'"* ซึ่งแก้อะไรไม่ได้ และฉุด Health Score

---

## 4. โครงหน้าใหม่ — 4 บล็อค

### บล็อค 0 — North Star (แถบบนสุด, CEO อ่านก่อน)

```
┌──────────────────────────────────────────────────────────────────────┐
│  สัปดาห์นี้:  200 สมัคร  →  18 ได้วิดีโอแรก  (9%)   ▼ -2% wow           │
│  ลูกค้าจ่ายเงิน 112 · ใช้งานได้จริง 31 (28%)  🔴 81 คนติดที่ยังไม่ตั้งคีย์ │
└──────────────────────────────────────────────────────────────────────┘
```
- **ตอบ:** ธุรกิจสุขภาพดีไหม ใน 2 วินาที
- **Data:** `User` count, `Video` distinct user COMPLETED, key coverage (ดู §6)

> 💡 **สำหรับ CEO — อ่านแล้วตัดสินใจอะไร:** ตัวเลขสุขภาพธุรกิจตัวเดียวที่ต้องดูก่อนทุกอย่าง ถ้า **"ได้วิดีโอแรก %"** ตก = ปัญหาใหญ่กว่า metric ระบบทุกตัวรวมกัน → ตัดสินใจทุ่มทรัพยากรไปที่ activation ก่อน ไม่ใช่ไล่แก้บั๊กปลีกย่อย

---

### บล็อค 1 — Activation funnel (เลนส์คน · หน่วย = distinct user)

```
ขั้น (นับเป็น "คน")            จำนวน   ผ่าน%   สาเหตุที่คนหาย
─────────────────────────────────────────────────────────────────────────
1. สมัคร                       200    100%    —
2. ตั้ง Gemini key (บังคับ)     48     24%    🔴 GATE: คน 76% ไม่ตั้งคีย์ → onboarding
3. เปิด editor                  97     —      (สำรวจก่อนตั้งคีย์ได้)
4. กดเริ่มสร้าง                  39     —      UX: ลอง editor แต่ไม่กดเริ่ม
5. ได้วิดีโอเสร็จ ≥1            18     46%*   🟠 *เริ่ม→เสร็จ หลุด 54% → เช็ค p95 บล็อค 2
6. ทำซ้ำ ≥2 (retention)         (TBD)  —      ติดใจหรือเปล่า
─────────────────────────────────────────────────────────────────────────
* 18/39 ของคนที่กดเริ่ม
```
- **ตอบ:** เสียคนตรงไหนกว่าจะได้วิดีโอแรก + เป็นเรื่อง "คีย์/UX" หรือ "ระบบ"
- **คอลัมน์ "สาเหตุที่คนหาย"** = สะพานไปเลนส์ระบบ: ขั้น 5 หลุด 54% → ชี้ไปบล็อค 2 (stage ไหน p95 สูง)
- **Data:** distinct `userId` ต่อขั้น (ดู §6) — **หน่วยเดียวกันหมด = เทียบกันได้จริง**

> 💡 **สำหรับ CEO — อ่านแล้วตัดสินใจอะไร:** บอกว่า "เสียลูกค้าตรงไหน" และที่สำคัญคือ **บอกด้วยว่าเป็นเรื่องที่แก้ได้ด้วยอะไร** — ขั้นที่หลุดเพราะ "คีย์/UX" แก้ด้วย onboarding/ดีไซน์ (ทีม product), ขั้นที่หลุดเพราะ "ช้า" แก้ด้วย perf (ทีม dev) ขั้นที่แดงสุด = จุดที่ลงเงิน/เวลาแล้วคุ้มสุด

---

### บล็อค 2 — Pipeline scorecard (เลนส์ระบบ · หน่วย = run) — **แทนกราฟแท่ง 7 ขั้นเดิม**

```
stage          runs  สำเร็จ%  error(ระบบ/คีย์)   p50      p95         ธง
──────────────────────────────────────────────────────────────────────────
หา B-roll       148    93%     0 / 0           91 วิ    24 นาที 🔴   หางยาว max 2.5ชม
เรนเดอร์         134    91%     2 / 0           3.4 นาที 18 นาที 🔴   + queue รอ tail 32นาที
ฝังซับ          92     82%     0 / 0(noise×10) 30 วิ    2 นาที       superseded=noise
หา keyword      147    96%     0 / 6           5 วิ     20 วิ        🟡 คีย์ลูกค้าไม่ผูกบัตร
สร้างเสียง       211    ~95%†   0 / 0           8 วิ     40 วิ        †succ จริง (client loss)
ถอดซับ          179    88%     0 / 0           10 วิ    30 วิ        ✅
จัดลำดับคลิป      131    100%    0 / 0           1 วิ     3 วิ         ✅
สร้าง Avatar     63     ~95%†   0 / 0           …        …            (เฉพาะงาน avatar)
──────────────────────────────────────────────────────────────────────────
† success ต้องอ่านจาก VideoJob ฝั่ง server (กฎ #2) ไม่ใช่ client event
```
- **ตอบ:** stage ไหน fail / ช้า → แก้ perf/reliability ถูกจุด
- **เลนส์ p95 = ตัวเชื่อมกับบล็อค 1**: stage p95 สูง = จุดที่คน abandon ทั้งที่ระบบไม่ error
- **Data:** `VideoJob` (server-authoritative) + `durationMs` จาก `*_server_*` events
- **ทิ้งกราฟแท่ง funnel 7 ขั้นเดิม** — ใช้ตารางนี้แทน (ข้อมูลมีอยู่แล้วใน `summarize().steps` `route.ts:433`, แค่ยังไม่ถูกเชิดขึ้นมา)

> 💡 **สำหรับ CEO — อ่านแล้วตัดสินใจอะไร:** อ่าน 2 คอลัมน์นี้ก็พอ — **p95 (หาง) = "ขั้นไหนทำลูกค้ารอจนถอดใจ"** → สั่งแก้ความเร็วตรงแถวแดง · **error = "ขั้นไหนพังจริง"** → สั่งแก้ความเสถียร แยกกันชัดเพราะ "ช้า" กับ "พัง" คนละวิธีแก้

---

### บล็อค 3 — Reliability แยก 3 กอง (+ Health Score v2)

```
┌─ 🔴 ระบบเรา (แก้โค้ด) ─────────┐ ┌─ 🟡 คีย์ลูกค้า (เด้งบอกลูกค้า) ─┐ ┌─ ⚪ noise (พับซ่อน) ──┐
│ render: webpack bundle ×4     │ │ keywords: Gemini ไม่ผูกบัตร ×6 │ │ superseded ×10        │
│ fetchStock: fetch failed ×1   │ │ tts/keywords: 429/503 ×5      │ │ play/pause race       │
│  → รวม ~5 ครั้ง/7วัน           │ │  → ไม่ใช่บั๊กเรา                │ │ opaque script error   │
└───────────────────────────────┘ └───────────────────────────────┘ └──────────────────────┘

Health Score v2 = คิดจากกอง 🔴 + render p95 + completion + งานค้าง เท่านั้น (ไม่เอา funnel/คีย์ลูกค้า/noise)
   ⚠️ ตัวเลขจะยังไม่สูงทันที เพราะ render p95 ยัง ~21 นาที (penalty เต็ม) + มี frontend error จริง ~14/สัปดาห์
   แต่ตอนนี้คะแนน "หมายถึงของจริง" และจะขึ้นเมื่อแก้ perf/frontend (ไม่ถูก byok/noise ฉุดอีก)
```
- **ตอบ:** บั๊กของเราเหลือกี่ตัวจริงๆ (น้อยมาก) + อะไรเป็นปัญหาฝั่งลูกค้าที่ต้องสื่อสาร
- **Data:** `pipeline_step_error` / `*_server_error` + classifier ใหม่ (ดู §6.3)

> 💡 **สำหรับ CEO — อ่านแล้วตัดสินใจอะไร:** แยกชัดว่าอะไรคือ **บั๊กของเรา (กอง 🔴 → สั่งทีมแก้โค้ด)** vs **ปัญหาคีย์ลูกค้า (กอง 🟡 → ทีม support สื่อสาร/ช่วยตั้งค่า ไม่ใช่งาน dev)** — จะได้ไม่สั่งทีมไล่แก้ของที่ไม่ใช่ปัญหาเรา และ Health Score ที่เห็นเป็นเลขจริง เชื่อแล้วตัดสินใจได้

---

## 5. Resource & playback (คงไว้ ย้ายไปท้ายหน้า / collapse)
ข้อมูลพวกนี้ดีและแม่นอยู่แล้ว — เป็น "ลิ้นชักลึก" ของบล็อค 2 ไม่ต้องเด่น:
- **Render queue**: avg wait 46 วิ, **max 32 นาที** (tail) · concurrency 4 · mem ว่าง 11 GB (สบาย)
- **B-roll phases**: search/download/normalize p95, cache-hit%, provider mix — ใช้ debug ตอน p95 สูง
- **Playback**: buffering%, first-frame p95 — เฉพาะตอนสืบปัญหาเล่นวิดีโอ
- **Web vitals**: LCP/INP/CLS p75 (⚠️ ตอนนี้มี outlier ทำให้ avg เพี้ยน — ใช้ p75 เท่านั้น)

---

## 6. กฎความน่าเชื่อถือ + การ implement

### กฎ #1 — หน่วยเดียวต่อบล็อค
บล็อค 1 = **คน** (distinct userId) · บล็อค 2 = **run** (VideoJob/pipelineRunId) — ห้ามผสม
> แก้ `summarizeEditorFunnel`: เลิกเอา raw-event-count (ขั้น1) ปนกับ run-dedup (ขั้น2-7) → บล็อค 1 ใช้ distinct user ทุกขั้น

### กฎ #2 — Server-authoritative
Stage done/fail ต้องอ่านจาก source ฝั่ง server ที่ไม่หายตอนปิดแท็บ:
- หลัก: `VideoJob.currentStep` / `status` / `startedAt` / `finishedAt` (server เขียน)
- เสริม: `render_server_*`, `fetch_stock_server_*` (มี `source='server'` อยู่แล้ว)
- ⚠️ เลิกพึ่ง client `pipeline_step_done` เป็นตัวนับ success (มันหายได้ — ดู §3.2)
> ระยะยาว: ให้ `orchestrator.ts` emit telemetry ฝั่ง server ต่อ stage (ตอนนี้ worker log ลง stdout เฉยๆ ไม่เขียน TelemetryEvent)

### กฎ #3 — ป้ายวันเริ่มเก็บ + เตือน window
- field ใหม่ติดป้าย เช่น *"pipelineRunId เริ่มเก็บ 18 มิ.ย. — ข้อมูลก่อนหน้าไม่มี run-id"*
- ถ้า window (เช่น 14 วัน) ยาวกว่าวันที่ field เริ่มมี → ขึ้น banner เตือน ไม่ให้อ่านเป็น 14 วันเต็ม
- fallback การ group run ใช้ `draftId`/`sessionId` ไม่ใช่ fallback ไปนับ event

### กฎ #4 — แยก error 3 กอง (ทั้งหน้า)
classifier ใหม่ `classifyError(row)` → `'system' | 'byok' | 'noise'`:
- **noise** = `benignTelemetryReason()` เดิม (superseded, play/pause, abort, opaque)
- **byok** = message เข้าเงื่อนไข: `429` | `503` | `quota` | `RESOURCE_EXHAUSTED` | `ยังไม่ได้ผูกบัตร` | `API key` | `unauthorized` | `invalid key`
- **system** = ที่เหลือ
> ใช้ทั้ง error list, recommendations, และ Health Score

### กฎ #5 — ภาษาคน + บอกการตัดสินใจ (บนหน้าจริง ไม่ใช่แค่เอกสาร)
หน้านี้คนอ่านคือ **CEO ที่ใช้ตัดสินใจ** ไม่ใช่ dev ที่อ่าน log → ทุก metric บนหน้าต้อง:
- **คงศัพท์มาตรฐาน** (p95, funnel, activation…) ไว้ได้ **แต่มี tooltip/`(?)` อธิบายภาษาคน** กำกับทุกตัว (เนื้อหาจาก Glossary ด้านบน)
- **ทุกบล็อค insight มีบรรทัด "อ่านแล้วตัดสินใจอะไร"** (เหมือนกรอบ 💡 ในเอกสารนี้) ไม่ใช่โยนตัวเลขเปล่าๆ
- ตัวเลข "ดิบสำหรับ dev" (เช่น phase timing, cache%, ms) → อยู่ใน **drawer ที่พับไว้** ไม่ปนกับชั้นสรุปที่ CEO อ่าน
> เป้าหมาย: CEO เปิดหน้าเดียวแล้ว **อ่านวิเคราะห์เอง → ตัดสินใจเอง** ได้โดยไม่ต้องถาม dev ว่าตัวเลขนี้แปลว่าอะไร

### Health Score v2 (เสนอแก้ `route.ts:482`)
```
100
 − systemErrorPenalty        (เฉพาะกอง 🔴 — ไม่เอา byok/noise)
 − renderP95Penalty          (p95 render/broll)
 − completionPenalty         (เฉพาะ run ที่เข้า pipeline แล้ว)
 − stuckPenalty              (PROCESSING ค้าง)
[ตัด: funnel dropoff, byok errors, noise ออกทั้งหมด]
```
ผลที่คาด: ยังไม่สูงทันที (render p95 21 นาที = penalty เต็ม + frontend error จริง ~14/wk) — แต่เลิกถูก byok/noise ฉุด, "หมายถึงของจริง", และจะขึ้นเมื่อแก้ perf. ประโยชน์หลักของ v2 = นิยามถูก + แยก 3 กองให้เห็นว่าบั๊กเราจริงๆ น้อยมาก (ไม่ใช่ตัวเลขกระโดด)

---

## 7. Data source map (สำหรับ dev)

| บล็อค | ตัวเลข | Source |
|---|---|---|
| 0 North Star | สมัคร | `User` count |
| | ได้วิดีโอแรก | `Video` distinct `userId` WHERE `status='COMPLETED'` |
| | paid ไม่มีคีย์ | `User` WHERE `plan IN (PRO,BUSINESS)` AND `geminiKey` ว่าง |
| 1 Activation | ตั้งคีย์ | `User` `geminiKey` ไม่ว่าง |
| | เปิด/เริ่ม | distinct `userId` ของ `editor_opened` / `editor_script_ready` |
| | วิดีโอเสร็จ/ซ้ำ | `Video` group by `userId`, นับ COMPLETED |
| 2 Scorecard | runs/สำเร็จ | `VideoJob` (หลัก) + `pipeline_step_*` (เสริม) |
| | p50/p95 | `durationMs` ของ `*_server_done` / `pipeline_step_done` |
| 3 Reliability | error 3 กอง | `pipeline_step_error`/`*_server_error` + `classifyError()` |
| 5 Resource | queue/mem | `render_server_started.properties` (`renderQueueWaitMs`,`freeMemGb`,`renderConcurrency`) |

ตาราง/field อ้างอิง: `TelemetryEvent(name, step, status, source, durationMs, properties, createdAt, userId, sessionId)` · `VideoJob(status, currentStep, startedAt, finishedAt)` · `Video(status, userId)` · `User(plan, geminiKey, pexelsKey, pixabayKey)`

---

## 8. แผนลงมือ (phased)

- **P0 (เล็ก, กันอ่านผิดทันที):** กฎ #1 (แก้หน่วยบล็อค1) + กฎ #3 (ป้ายวันเริ่ม/เตือน window) → ลบ "96% ปลอม"
- **P1 (insight ใหม่ที่ขาดหาย):** บล็อค 0 + บล็อค 1 Activation (จาก `User`/`Video`) → เห็น 9% activation + 72% no-key
- **P2:** บล็อค 2 scorecard จาก `VideoJob` (กฎ #2) + บล็อค 3 error 3 กอง (กฎ #4) + Health Score v2
- **P3 (ระยะยาว):** server-side stage telemetry ใน `orchestrator.ts`

> หมายเหตุ: P0–P2 เป็นงานในไฟล์ `src/app/api/admin/insights/route.ts` + `src/app/(dashboard)/admin/insights/page.tsx` เป็นหลัก (read-only ต่อ pipeline จริง — ไม่กระทบ render)

---

## Appendix A — Baseline จริง (prod snapshot 2026-06-18)

**คน:** 200 users (88 FREE / 110 PRO / 2 BUSINESS) · มี Gemini key 48 (24%) · มี stock key 43 · **paid ไม่มี Gemini key 81/112 (72%)**
**Activation:** เปิด editor 97 → กดเริ่ม 39 → ได้วิดีโอเสร็จ **18 คน (9% ของ 200)**
**Video:** COMPLETED 121 (18 users) · PROCESSING 2
**Pipeline (7วัน, started/done):** keywords 147/141 · tts 211/132(+45 skip) · transcribe 179/157 · fetchStock 148/137 · config 131/131 · render 134/122 · burnSubtitles 92/75 · avatar 63/43
**Latency (7วัน):** B-roll p50 91วิ / **p95 1422วิ (24นาที)** / max 8855วิ · render p50 202วิ / **p95 1092วิ** · render queue avg 46วิ / **max 1934วิ (32นาที)** · mem ว่าง 11GB
**Error จริง (7วัน หลังแยกกอง):** ระบบ ~5 (render webpack ×4, fetchStock ×1) · คีย์ลูกค้า ~11 (Gemini ไม่ผูกบัตร ×6, 429/503 ×5) · noise (superseded ×10)
**instrumentation:** `pipelineRunId` เริ่มเก็บ **18 มิ.ย. 07:58 น.** · telemetry dashboard เริ่ม 7 มิ.ย. (commit `0dc07bb`)

> ⚠️ ตัวเลข "ทำซ้ำ ≥2" ในบล็อค 1 ยังไม่ได้ query — ต้องนับ `Video` group by user having count≥2 ตอน implement
